import {useMemo,useState} from 'react';import {Search,SlidersHorizontal,Music2,ShoppingBag,ArrowUpRight} from 'lucide-react';import {artists,householdCats,indiaCats,receiptExamples} from '../data/insights';
const all=[...artists.map(([name,count])=>({type:'Music',title:name,detail:`${count.toLocaleString()} listening receipts`,period:'2013–2024'})),...householdCats.map(([name,count])=>({type:'Household',title:name,detail:`${count.toLocaleString()} receipts`,period:'2015–2018'})),...indiaCats.map(([name,count])=>({type:'Transactions',title:name.replaceAll('_',' '),detail:`${count.toLocaleString()} transactions`,period:'2022–2024'})),...receiptExamples.map(x=>({type:x.type==='music'?'Music':'Spending',title:x.title,detail:x.detail,period:x.date}))];
export default function Explore(){const[q,setQ]=useState('');const[type,setType]=useState('All');const filtered=useMemo(()=>all.filter(x=>(type==='All'||x.type===type)&&(`${x.title} ${x.detail} ${x.period}`).toLowerCase().includes(q.toLowerCase())),[q,type]);return <main className="mx-auto max-w-7xl px-5 py-12"><div className="max-w-2xl"><div className="text-xs font-semibold uppercase tracking-[.2em] text-blue-300">Explore</div><h1 className="mt-2 text-4xl font-semibold tracking-tight">Search the receipts.</h1><p className="mt-3 text-slate-400">Explore aggregated records from all three supplied datasets without exposing raw personal identifiers.</p></div><div className="mt-8 glass rounded-2xl p-3"><div className="flex flex-col gap-3 md:flex-row"><label className="relative flex-1"><Search className="absolute left-4 top-1/2 -translate-y-1/2 text-slate-500" size={18}/><input value={q} onChange={e=>setQ(e.target.value)} aria-label="Search receipts" placeholder="Search artists, categories, patterns..." className="focus-ring w-full rounded-xl border border-white/10 bg-white/5 py-3 pl-11 pr-4 text-sm text-white placeholder:text-slate-600"/></label><div className="flex items-center gap-2 overflow-auto">{['All','Music','Household','Transactions'].map(t=><button key={t} onClick={()=>setType(t)} className={`focus-ring whitespace-nowrap rounded-xl px-4 py-3 text-sm ${type===t?'bg-white text-slate-950':'bg-white/5 text-slate-300 hover:bg-white/10'}`}><SlidersHorizontal className="mr-2 inline" size={14}/>{t}</button>)}</div></div></div><div className="mt-7 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{filtered.map((x,i)=><article key={`${x.title}-${i}`} className="glass rounded-2xl p-5"><div className="flex items-center justify-between"><span className="inline-flex items-center gap-2 rounded-full bg-white/5 px-2.5 py-1 text-xs text-slate-400">{x.type==='Music'?<Music2 size={13}/>:<ShoppingBag size={13}/>} {x.type}</span><ArrowUpRight size={16} className="text-slate-600"/></div><h2 className="mt-6 text-lg font-semibold capitalize text-white">{x.title}</h2><p className="mt-1 text-sm text-slate-400">{x.detail}</p><p className="mt-5 text-xs text-slate-500">{x.period}</p></article>)}</div>{!filtered.length&&<div className="py-20 text-center text-slate-500">No matching receipt patterns found.</div>}</main>}
